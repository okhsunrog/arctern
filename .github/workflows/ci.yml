name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  CARGO_TERM_COLOR: always

jobs:
  rust:
    name: Rust
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          path: arctern

      # arctern depends on zfskit via a sibling path (../zfskit).
      - uses: actions/checkout@v6
        with:
          repository: okhsunrog/zfskit
          path: zfskit

      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: arctern

      # build.rs embeds admin-ui/dist via memory_serve; an empty dir is enough
      # to compile (integration tests are gated behind the `integration` feature
      # and need real ZFS/SSH, so they stay off here).
      # build.rs embeds admin-ui/dist via memory_serve (empty dir is fine), and
      # router.rs `include_bytes!`s dist/index.html for the SPA fallback — so a
      # stub index.html must exist to compile without building the real UI.
      - run: mkdir -p admin-ui/dist && printf '<!doctype html><title>arctern</title>' > admin-ui/dist/index.html
        working-directory: arctern
      # Scope fmt to arctern's own packages: `--all` also pulls in the
      # zfskit path dependency, which has its own repo and CI and must not
      # gate arctern. clippy/test below use `--workspace` (members only).
      - run: cargo fmt --check -p arctern-api -p arctern-client -p arctern-config -p arctern-transport -p arctern-daemon
        working-directory: arctern
      - run: cargo clippy --workspace -- -D warnings
        working-directory: arctern
      - run: cargo test --workspace
        working-directory: arctern

  frontend:
    name: Frontend
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: voidzero-dev/setup-vp@v1
        with:
          cache: true
          working-directory: admin-ui

      - run: vp check
        working-directory: admin-ui
