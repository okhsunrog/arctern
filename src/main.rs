// Implementation is blocked on the palimpsest sibling crate providing
// the ZFS operations arctern needs. See .specify/memory/constitution.md
// for the design and CLAUDE.md for conventions.

fn main() {
    eprintln!("arctern is in design. See .specify/memory/constitution.md.");
    std::process::exit(1);
}
