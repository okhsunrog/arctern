[workspace]
resolver = "3"
members = [
    "crates/api",
    "crates/client",
    "crates/config",
    "crates/transport",
    "daemon",
]

[workspace.package]
version = "0.1.0"
edition = "2024"
license = "MIT"
publish = false

[profile.release]
strip = true
lto = "thin"
